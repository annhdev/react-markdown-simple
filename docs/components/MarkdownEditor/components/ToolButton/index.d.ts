import { default as React } from '../../../../../node_modules/react';
declare const ToolButton: ({ icon, onClick, tooltip, disabled }: {
    icon: React.ReactNode;
    onClick: () => void;
    tooltip: string;
    disabled: boolean;
}) => import("react/jsx-runtime").JSX.Element;
export default ToolButton;
